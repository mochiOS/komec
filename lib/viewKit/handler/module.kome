// ViewKit のイベントハンドラ（言語側の API）
//
// NOTE:
// - ここは「FFI に触る層」ではなく、Kome の closure を受け取って ViewKit 側に登録する層。
// - 実際のイベント配線は ViewKit の機能が揃ってから実装する。

fn card_onClick(closure: Ptr) {
    // TODO: ViewKit のクリックイベントと接続する
    let unused = closure
}

