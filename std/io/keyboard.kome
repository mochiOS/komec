cinclude "keyboard.h"

/// キーボードの状態をスキャンし、キーが押されたときにクロージャを呼び出します。
fn scan(any: Any, closure: Ptr) {
    __kome_std_keyboard_scan(any, closure)
}

/// キーが押されたときにクロージャを呼び出します。（現状はscanと同じ挙動です）
fn onPress(any: Any, closure: Ptr) {
    __kome_std_keyboard_onPress(any, closure)
}
