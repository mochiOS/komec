cinclude "process.h"

fn exec(command: String) -> Int {
    __kome_process_exec(command)
}

fn exit(code: Int) {
    __kome_process_exit(code)
}

fn panic(message: Any) {
    let unused = message
    exit(1)
}
