struct Point {
    x: Number,
    y: Number,
}

struct Empty {}

@runtime("string")
struct RuntimeString

@native("core.write_line")
fn write_line(value: RuntimeString)

fn main() {
    let message: RuntimeString = "struct declarations work"
    write_line(message)
}
