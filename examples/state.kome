use std.io
use std.io.keyboard
use std.bundle

bundle App {
    public state let mut Index = 0

    recipe button: Index = {
        println("Button pressed: %d", Index)
    }
}

fn main() {
    keyboard.scan(any) {
        App.Index += 1          // キーが押されるたびにIndexをインクリメント
    }

    App.run()
}
