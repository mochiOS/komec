use std.io
use std.bundle

bundle App {
    public state let mut Index = 0

    recipe button: Index = {
        io.println("Button pressed: %d", Index)
    }
}

fn main() {
    keyboard.scan(_) {
        App.Index += 1          // キーが押されるたびにIndexをインクリメント
    }

    App.run()
}
